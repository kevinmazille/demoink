# Throwaway POC — kept locally for reference, never committed (Kevin's call).
POC/

# macOS / Xcode cruft
.DS_Store
xcuserdata/
*.xcuserstate
DerivedData/
build/

# Packaging output (package.sh) — build artifacts, never committed
dist/
